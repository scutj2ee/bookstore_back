<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scutj2ee.bookstore.dao.OrderDao">
  <resultMap id="orderMap" type="com.scutj2ee.bookstore.entity.Order">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="address_id" jdbcType="INTEGER" property="addressId" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="end_time" jdbcType="TIMESTAMP" property="endTime" />
    <result column="state" jdbcType="INTEGER" property="state" />
    <result column="payment" jdbcType="DOUBLE" property="payment" />
    <result column="total_integral" jdbcType="INTEGER" property="totalIntegral" />
    <result column="user_id" jdbcType="INTEGER" property="userId" />
    <result column="is_delete" jdbcType="BOOLEAN" property="idDelete" />
  </resultMap>

  <delete id="deleteOrder" parameterType="java.lang.Integer">
    delete from order
    where id = #{id}
  </delete>

  <insert id="insertOrder" parameterType="com.scutj2ee.bookstore.entity.Order">
    insert into order (address_id, create_time, end_time, `state`,
      payment, total_integral, user_id, is_delete)
    values (#{addressId}, #{createTime}, #{endTime}, #{state},
      #{payment}, #{totalIntegral}, #{userId}, #{isDelete})
  </insert>

  <update id="updateOrder" parameterType="com.scutj2ee.bookstore.entity.Order">
    update order
    <set>
      <if test="addressId != null and addressId != ''">address_id = #{addressId},</if>
      <if test="createTime != null and createTime != ''">create_time = #{createTime},</if>
      <if test="endTime != null and endTime != ''">end_time = #{endTime},</if>
      <if test="state != null and state != ''">`state` = #{state},</if>
      <if test="payment != null and payment != ''">payment = #{payment},</if>
      <if test="totalIntegral != null and totalIntegral != ''">total_integral = #{totalIntegral},</if>
      <if test="userId != null and userId != ''">user_id = #{userId},</if>
      <if test="isDelete != null and isDelete != ''">is_delete = #{isDelete}</if>
    </set>
    where id = #{id}
  </update>

  <select id="findOrderById" parameterType="java.lang.Integer" resultMap="orderMap">
    select id, address_id, create_time, end_time, `state`, payment, total_integral, user_id, is_delete
    from order
    where id = #{id}
  </select>

  <select id="selectAll" resultMap="orderMap">
    select id, address_id, create_time, end_time, `state`, payment, total_integral, user_id,is_delete
    from order
  </select>

  <select id="findByUserId" parameterType="java.lang.Integer" resultMap="orderMap">
    select id, address_id, create_time, end_time, `state`, payment, total_integral, user_id,is_delete
    from order
    where user_id = #{userId}
  </select>

  <select id="getOrderListByParams" resultMap="bookInfoMap">
    select o.*
    from order o
  </select>
</mapper>